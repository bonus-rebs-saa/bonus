spring:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:bonus;DB_CLOSE_DELAY=-1
    username: bonus
    password: bonus
  liquibase:
    enabled: false